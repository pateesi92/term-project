name: PR Validation

on:
  push:
    branches:
      - develop
      - feature/contact-page  # ✅ Your branch
  pull_request:
    branches:
      - develop

jobs:
  lint-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run HTMLHint
        run: npx htmlhint "**/*.html"

      - name: Run Stylelint
        run: npx stylelint "**/*.css"

      - name: Build with Parcel
        run: npm run build
